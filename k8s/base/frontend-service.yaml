apiVersion: v1
kind: Service
metadata:
  name: picopals-frontend
  namespace: picopals
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector: { app: picopals-frontend }
  ports:
    - port: 80
      targetPort: 80
